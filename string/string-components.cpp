// 1. strcat = concatenation(adding of two values)
// 2. strcpy = strcpy(s1, s2)
// 3. strlen = returns the length of the given string(strlen(s1, s2))
// 4. strcmp = use of comparing two strings(strcm(s1, s2)) 
// Also in strcmp the return value is a string , if s1 > s2 ? it returns a 
// negative value and for s1 < s2 ? if returns positive value and if same then
// 0. 
// ex : strcmp(s1, s2)
// s1 = abc, s2 = pqr;
// it will return a negative value as per the difference in the ascii value
// 5. strcmpi = where i stands for ignore.
// it ignores the case of the alphabets.
// 6. strchr = it will return the pointer to the first occurence of given
// character in the given string.

// ex: strchr(str1, str2)
// str1 = "abcde", chr = 'c'
// it will print cde as the value will be returned from the first occurence of the string./

// strcpy(s1, s2, 4)
// copies the first four chars from the s2 and print it in the first string.

