#include <iostream>
#include <string>
using namespace std;

int main(){ // array of characters accessed by using substantial or index

    char str1[40]; // array of str1 of size 40;
    cout << "Enter the string: ";
    for(int i = 0; i < 40; i++)}{
        cin >> str1[i];
    }
    // and a for loop to print the output of the code;;
    return 0;
}